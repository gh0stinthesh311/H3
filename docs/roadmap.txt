


*done
if no column do not let add the value for row 
cycle now does add to a table rows many times , only one time should be
move sql keywords to enum and get them by key or name



*****
logger SLF4j + Logback 

I go by popularity and ease of use. Our projects are using SLF4J and Logback, and they work well.
*****
make creation of table only possible with database attached

make id impossible to be null (constructor)
make basic parser for sql statements
Create constructor for table so table can not be created with different types
make id unique
add logger

make id unique
make id non null

make dimensions of tables final
 

logger
 property reader